/** Rendered pixel size of one tile (source image dimensions). */
export const TILE_SIZE = 16;
/** Physical world tile size in pixels — the collision grid cell size. Always TILE_SIZE * 2. */
export const PHYSICAL_TILE_SIZE = TILE_SIZE * 2;
